syntax = "proto3";

package insulter;

option go_package = "insulter/grpc/proto";

service insulter {
    rpc insult(Value) returns (Value) {}
 }

message Value {
    string value = 1;
}

message Value {
   string value = 2;
}